function tres 

   # global PACKET_LENGTH ;
  #global PACKETS_PER_PRBS_PERIOD; 
  #global SYNC_BYTE_VALUE INV_SYNC_BYTE_VALUE;
  #global INIT_PRBS_REGISTER;
  #global RS_PARITY_BYTES;
  #global OI_I OI_M;
  #global INIT_CONV_REGISTER;
  #global PUNCTURING_MODE;
  #global QAM_MODE LOG_QAM_MODE;
  global VAR;
  global GLB;
  GLB =3;
  VAR.x = 6
  VAR.b.c = 5 + GLB;
  v = 1
 # a = VAR.x + VAR.b.c
  VAR.array = [3,4,5]
  a = VAR.array(1)++ 

  VAR.array(v++,4) = 7
  rr = zeros(v,v++)
  m = ["as","b"]
  mat = [m]

  # b=8
  # b++
  #cuatro(8)


 
 # f = is_scalar('sfd')
  # ff = [1,2,3; 4,5,6]
  # g = ff(:)
  ## m = ff(1,3)
 ##  n = ff(1, 2)
 ##  o = ff(1,:)
  ## p = ff(:,2)
   # c = 1
   # d = 1
   # q = ff(1:1:c+d, 1)

  #s = svd([1.2,3.1; 3.4,1.3434])
  #[h,i,j]  = svd([1.2,3.1; 3.4,1.3434])

   #  rrrrrrrr = rows([1,2])
   # [ee,rr] = size([1,2])
   # ffxxxxxx = diff([1,2,8],1)
   #ffffffff = flipud([1,2])
   #reg = ones (11 )
   #ruf = ones(1,11)
   #raf = ones(11,2)


  

   #d = '47'
   #d = strjust ( d , 'right' )
    #dfi = do_fortran_indexing
    #idx = find([5,5,3])
    #x ( idx ) = ' '  
    #idx = x' != ' '
    #[ nr , nc ] = size ('hola')
    #rr = strcmp( 'hola' , 'right' )
   #table = (NaN * ones ( 256 , 1 ))
